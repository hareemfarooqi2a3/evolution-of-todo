apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatbot-frontend.fullname" . }}
  labels:
    {{- include "chatbot-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatbot-frontend.selectorLabels" . | nindent 4 }}
