apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "reminders-service.fullname" . }}
  labels:
    {{- include "reminders-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "reminders-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- if .Values.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: {{ .Values.dapr.appId | quote }}
        dapr.io/app-port: {{ .Values.dapr.appPort | quote }}
        {{- end }}
      labels:
        {{- include "reminders-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "reminders-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.env.KAFKA_BOOTSTRAP_SERVERS | quote }}
            - name: KAFKA_TOPIC
              value: {{ .Values.env.KAFKA_TOPIC | quote }}
            - name: KAFKA_GROUP_ID
              value: {{ .Values.env.KAFKA_GROUP_ID | quote }}
            - name: NOTIFICATION_METHOD
              value: {{ .Values.env.NOTIFICATION_METHOD | quote }}
            {{- if .Values.env.SMTP_HOST }}
            - name: SMTP_HOST
              value: {{ .Values.env.SMTP_HOST | quote }}
            - name: SMTP_PORT
              value: {{ .Values.env.SMTP_PORT | quote }}
            - name: SMTP_USER
              value: {{ .Values.env.SMTP_USER | quote }}
            - name: SMTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: todo-secrets
                  key: SMTP_PASSWORD
                  optional: true
            - name: SMTP_FROM
              value: {{ .Values.env.SMTP_FROM | quote }}
            {{- end }}
            {{- if .Values.env.WEBHOOK_URL }}
            - name: WEBHOOK_URL
              value: {{ .Values.env.WEBHOOK_URL | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
